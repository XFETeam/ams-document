main.home {
  height: 100vh;
  padding-bottom: 20vh;
  box-sizing: border-box;
  display: flex;
  align-items: center;
  justify-content: center;
}

main.home .footer {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
}
